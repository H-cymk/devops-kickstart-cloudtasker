name: CI Pipeline

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Simulate Build
      run: |
        echo "Starting simulated CI pipeline..."
        echo "All features are in develop"
        echo "Simulated tests complete ✅"
